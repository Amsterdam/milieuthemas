[uwsgi]
plugins = consul
http = :8000
module = milieuthemas.wsgi:application

master = true
static-map = /static=/static

consul-register = url=http://$(CONSUL_HOST):$(CONSUL_PORT),name=milieuthemas-api,port=8106,address=$(DOCKER_EXTERNAL_IP),ttl=10